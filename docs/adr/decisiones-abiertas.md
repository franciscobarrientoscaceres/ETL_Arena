# Registro de decisiones abiertas (D-01 … D-10)

Origen: `.kiro/specs/etl-arena-availability/audit.md` §4. Cada decisión tiene un **default de paridad** que permite avanzar la Fase 1 sin esperar la resolución. Al resolver una, actualizar **Estado**, **Resolución** y **Fecha**, y propagar el cambio a `requirements.md`/`design.md` si altera el comportamiento.

| ID | Decisión | Default v1 (paridad) | Dueño | Estado | Resolución | Fecha |
|---|---|---|---|---|---|---|
| D-01 | Filas después de la primera celda vacía en `RawData-PCS!A` (F-10) | Truncar como Excel (`modo_huecos = "excel"`) y reportar | Técnico | Aceptado default | — | 2026-09-24 |
| D-02 | Unión con `PlantActivity` y `Exclusion_Matrix` (F-01, F-37) | Por número de fila; vacío = 0; reportar desalineación | Técnico | Aceptado default | Aplica igual a `Exclusion_Matrix` | 2026-09-24 |
| D-03 | ¿`ListOfFaults!L14` (eventos) debe igualar a `C31` (KPI)? Hoy `L14 = "Yes"`, `C31 = "No"` (F-05) | Parámetros separados; el runner escribe ambos explícitamente | **Negocio** | **Resuelta** | El KPI oficial **descuenta** eventos excusables: `C31 = L14 = "Yes"`. Evidencia: el KPI oficial de julio en `Annual_AVA` (350.972,300) coincide exacto con el cálculo excusando. Los parámetros siguen separados en `ConfiguracionCalculo` (paridad), pero la corrida oficial usa ambos en `Yes`. **Desde F-37 el flag activa `Exclusion_Matrix`** (no `PlantActivity!D`) | 2026-09-24 |
| D-04 | Bug de arrastre de eventos al cierre del período (F-06) | Replicar y marcar `EventoArrastradoExcel`; corregir en v2 | **Negocio** | **Resuelta** | Replicar en v1 con flag `EventoArrastradoExcel` (= default) | 2026-09-24 |
| D-05 | Daily sin factor operacional (F-08) | Replicar | Técnico | Aceptado default | — | 2026-09-24 |
| D-06 | Inicio de la acumulación anual (F-20) | Meses con KPI oficial (hoy desde jul-2026, como el libro); backfill abr–jun opcional | **Negocio** | **Resuelta** | Acumulación desde julio 2026, como el libro; sin backfill abr–jun | 2026-09-24 |
| D-07 | Período que calcula cada lunes y cierre mensual (F-28) | Mes en curso hasta el último domingo; el primer lunes del mes cierra también el mes anterior | **Negocio** | **Resuelta** | (1) Export semanal **incremental**: desde el dato siguiente al último cargado (hoy 2026-09-21 14:15) hasta el último dato obtenido manualmente ese lunes (no hasta el domingo). (2) KPI semanal = **acumulado del mes en curso**: `C5` = día 1 del mes del último dato, `C7` = fecha del último dato. (3) Cierre mensual = mes completo (día 1 00:00 … último día 23:45), con denominador = **intervalos existentes** (`C12`, filas), no bloques de calendario | 2026-09-24 |
| D-08 | Evento que inicia en la fila 2 (en Excel, *Type mismatch*) (F-11) | Abrir el evento y marcar `ExcelHabriaFallado` | Técnico | Aceptado default | — | 2026-09-24 |
| D-09 | Retención de muestras por corrida (F-28) | Raw completo del export con columnstore; `availability_sample_result` solo del período | Técnico/DBA | Aceptado (ajustado por D-07) | Con export incremental, `raw_pcs_sample` y `plant_activity_sample` guardan las filas **del período calculado** (mes en curso, ≤ ~3.000 filas × 61 PCS) con marca `EsFilaNuevaDelExport`; no la historia completa del libro | 2026-09-24 |
| D-10 | Versión de Python (F-29) | `requires-python >= 3.13` | Técnico | **Resuelta** | 3.14 en el PC de oficina, 3.13 en FRANCISCO-PC; ambos soportados | 2026-09-24 |

## Respuestas de negocio (tarea 0.2, 2026-09-24, Francisco)

1. **D-03:** los dos flags en `"Yes"`: el KPI oficial descuenta los eventos excusables y la lista de fallas también.
2. **D-04:** replicar el arrastre en v1, con flag.
3. **D-06:** acumulado anual desde julio 2026, como el libro.
4. **D-07:** export incremental semanal; KPI semanal = acumulado del mes en curso hasta el último dato; cierre mensual = mes completo con denominador = intervalos existentes.

## Abiertas nuevas

| ID | Tema | Estado |
|---|---|---|
| D-11 | El KPI oficial de agosto en `Annual_AVA` (305.182,968) no se reproduce ni excusando (256.223,356) ni sin excusar (527.396,044) con el libro actual. Hipótesis: `PlantActivity!D` cambió después del cierre de agosto. Con la regla nueva (F-37) agosto da 264.731,768: tampoco coincide. Consultado a Alex (2026-09-24), sin respuesta aún. Julio sí se reproduce con la regla histórica (C31 = "Yes" con `PlantActivity!D`: 350.972,3 = `Annual_AVA!F13`, F-42), lo que refuerza que en agosto cambió algo después del cierre. Investigación 2026-09-25 (F-43): ninguna regla conocida (×D, ×C, ×D×C, matriz), ni cortes por fecha de las excusas, ni ventanas de días reproducen 305.182,968 con los libros disponibles; RawData de agosto es igual en ambos libros y lo que difiere es `PlantActivity!D`. Ese análisis es solo diagnóstico del número histórico: no cambia la regla vigente (exclusiones solo desde `Exclusion_Matrix`; `PlantActivity` no se usa para exclusiones hasta que Alex confirme). Pregunta concreta para Alex: ¿de qué libro sale el agosto oficial? Opciones para cerrar D-11: (a) mantener 305.182,968 como `excel_manual` (hoy) o (b) reemplazarlo por el cierre de agosto "Con Exclusiones" con la `Exclusion_Matrix` (264.731,768), si negocio lo aprueba. Se importa como `excel_manual` (R10.4); investigar con el libro de cierre de agosto | **Postergada** (2026-09-24): prioridad septiembre; Francisco solicitará el libro de cierre de agosto |
| D-12 | Quién actualiza `PlantActivity` (columnas B/C/D) para las filas nuevas | **Resuelta, alcance reducido por F-37** (2026-09-24): PlantActivity ya solo aporta la columna C ("Only Operational Time?", sin uso hoy); la exclusión la da `Exclusion_Matrix` (D-17). Original: la entrega **Alex** (ingeniero eléctrico a cargo) **una vez al mes**. Consecuencias: (1) ~~las corridas semanales son preliminares~~ — reemplazado por D-17: son oficiales "Sin Exclusiones"; (2) el `cierre_mensual` oficial se ejecuta después de cargar la `Exclusion_Matrix` del mes (etapa `load-exclusion-matrix`), que deja registro de cada celda cambiada en `correccion_dato`. Formato del archivo de Alex: pendiente de muestra (tarea 0.8) |
| D-14 | Criticidad (`Meaning`) de F228, F230, F231 y F232: el catálogo `PCS-Fault` los trae dos veces, como `Crítico` y como `Parcial` (F-35). El seed de `tipo_detencion` necesita un valor por código. Default propuesto: `Crítico` (el más conservador) | Pendiente (negocio / Alex) |
| D-13 | Corrección de datos ya cargados | **Resuelta** (2026-09-24): la carga semanal nunca reemplaza filas (solape distinto → rechazo). La corrección se hace con una corrida `reproceso` que escribe el tramo corregido en una **nueva** copia del libro y registra **cada celda cambiada** (fila, timestamp, PCS, campo, valor anterior, valor nuevo, archivo) en `correccion_dato` y en `data/work/<corte>/cambios.csv`; el libro base y las corridas anteriores no se tocan. Requisito de Francisco: poder ver qué datos cambiaron |
| D-15 | Definición del valor 2 de `Exclusion_Matrix` (F-37) | **Resuelta** (negocio, 2026-09-24): módulos en operación **antes** del inicio del EE. Implementación: fila anterior al tramo contiguo de 2 (C3 − M si tenía falla sin excluir; 0 si completa, vacía o valor 1). Coincide con la macro de agosto en todo el libro | — |
| D-16 | ¿Los eventos de falla (`ListOfFaults`, L14) también aplican `Exclusion_Matrix`? | **Resuelta** (negocio, F. Barrientos, 2026-09-24): sí, misma regla que el KPI para que `C14 = 4·L10`. La macro de agosto no lo hacía (usaba `PlantActivity!D`) | 2026-09-24 |
| D-17 | Quién mantiene la `Exclusion_Matrix` y cuándo se entrega (reemplaza en la práctica a la entrega mensual de PlantActivity de D-12) | **Resuelta** (negocio, 2026-09-24): la entrega **Alex una vez al mes, al final del mes**, para cargar el KPI mensual completo. Las corridas semanales **no son preliminares**: son **oficiales "Sin Exclusiones"** (`EstadoExclusiones = sin_exclusiones`). Al cargar la matriz del mes (`load-exclusion-matrix`) se ejecuta el `cierre_mensual` oficial "Con Exclusiones", que pasa a ser el KPI vigente del mes | 2026-09-24 |
| D-18 | Marca aislada en `Exclusion_Matrix` fila 2 (2026-04-08 00:15): PCS61 = 1 y "Excused Event" = 1 (F-39) | **Se mantiene** (F. Barrientos, 2026-09-24: "creo que debería quedar"); consultado a Alex, confirmar | Pendiente confirmación Alex |
| D-19 | Referencia Excel para las corridas "Con Exclusiones": las macros oficiales (septiembre) no conocen `Exclusion_Matrix` | **Resuelta** (F. Barrientos, 2026-09-24): opción (a) — nuevo libro maestro **v1.1** = libro de septiembre + hoja `Exclusion_Matrix` + la regla de la matriz en `cmdCalcAvailability` (tal cual la macro de agosto) y en `mcoCreateList` (D-16); todo lo demás de septiembre intacto. Lo prepara un humano, no el pipeline (tarea 4.0); se valida contra el libro de agosto y contra los motores Python | 2026-09-24 |
| D-20 | **Unidad de recarga (ADR-12):** ¿qué se reemplaza al volver a cargar un período? | **Resuelta** (F. Barrientos, 2026-09-26, "apruebo todas"): el **mes calendario**. El inicio se ajusta al día 1 (el VBA corta los eventos en el borde del período: recargar desde un día intermedio partiría las detenciones que lo cruzan); un rango de varios meses se procesa mes a mes; cada carga reemplaza todo lo del mes (muestras, detenciones, día a día, KPI, calidad) |
| D-21 | ¿Una recarga puede dejar un mes con menos datos que los ya cargados? | **Resuelta** (F. Barrientos, 2026-09-26, "apruebo todas"): no sin `--permitir-recorte` (si el último dato nuevo es anterior al último dato vigente del mes, la carga se detiene) |
| D-22 | ¿Qué cargas reemplazan los datos vigentes? | **Resuelta** (F. Barrientos, 2026-09-26, "apruebo todas"): solo las que terminan `success` (incluye `sin_referencia`). `parity_failed` queda en `etl_run` y `reconciliation_result` sin tocar el estado, salvo `--publicar-aunque-no-cuadre`. `golden` y `--sin-bd` nunca publican. `--oficial` deja de tener efecto (en PROD toda carga `success` publica; practicar en TEST/QA) |
| D-23 | Protecciones de meses especiales | **Resuelta** (F. Barrientos, 2026-09-26, "apruebo todas"): un mes `excel_manual` (jul/ago 2026) solo se reemplaza con `--reemplazar-manual` (ligado a D-11); un mes "Con Exclusiones" no baja a "Sin Exclusiones" sin `--forzar-sin-exclusiones` (así se llamó al implementarlo, para no chocar con `--forzar`, que repite etapas) |
| D-24 | Qué historia se conserva sin copias por corrida | **Resuelta** (F. Barrientos, 2026-09-26, "apruebo todas"): `etl_run` (una fila por ejecución: archivo, sha256, parámetros, resultado, reconciliación resumida), `correccion_dato` (cada dato crudo de SCADA o de la matriz que cambió al recargar: antes → después), `exclusion_matrix_carga`, `detencion_revision`, `excel_reference_run` (solo KPI) y `reconciliation_result` (resumen por nivel + diferencias). Cada fila vigente guarda `NumCorrida`. La referencia Excel detallada queda en `referencia_excel.json` del corte |
| D-25 | Identidad de una detención entre recargas | **Resuelta** (F. Barrientos, 2026-09-26, "apruebo todas"): clave de negocio `(IdProyecto, NumeroPCS, FechaInicio, Ocurrencia)`; `MERGE`: se actualizan las que siguen (mismo `IdDetencion`), se insertan las nuevas y se eliminan las del mes que ya no aparecen; `detencion_revision` sigue por clave y sobrevive |
| D-26 | Esquema v2 y qué lee Power BI | **Resuelta** (F. Barrientos, 2026-09-26, "apruebo todas"): 6 tablas de estado (`muestra_pcs`, `muestra_planta`, `detencion`, `disponibilidad_diaria`, `disponibilidad_mensual`, `calidad_dato`) + vistas `v_disponibilidad_anual`, `v_resumen_codigo_mensual`, `v_detencion`; Misael lee esas tablas y vistas. Se eliminan las copias por corrida y las vistas `v_*_vigente` |
| D-27 | Correcciones de datos de SCADA (antes "reproceso", D-13) | **Resuelta** (F. Barrientos, 2026-09-26, "apruebo todas"): recargar el mes con el export corregido: el libro de trabajo reemplaza las filas con el mismo timestamp (4.7) y cada diferencia queda en `correccion_dato`; la etapa `--reproceso` (4.13) se absorbe |
